const deviceSizes = {
    mobile: '576px',
    tablet: '992px',
};

const device = {
    mobile: `(max-width: ${deviceSizes.mobile})`,
    tablet: `(max-width: ${deviceSizes.tablet})`,
}

export default device;
